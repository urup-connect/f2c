# Copy to .env.local and adjust. Never commit .env.local.
# Real QA and Production values live in App Service application settings, not here.

# Which deployment this is: local | qa | production.
# Search engine indexing is permitted only when this reads "production".
APP_ENV=local

# Absolute origin this deployment is served from. Scheme and host only, no path.
SITE_URL=http://localhost:3000

# Root of the static content host this deployment reads from. Scheme, host and path; no trailing
# slash needed. The club documents hang off it under /documents, and future media will share it.
# Plain http is refused unless APP_ENV is local.
CDN_BASE_URL=https://qa-static.example.invalid/consumer-collective
